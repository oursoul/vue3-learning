<template>
  <div class="mt-5">
    composition api
  </div>
</template>

<script lang="ts">
import { defineComponent } from 'vue'

export default defineComponent({
  // 第一个参数props(父组件传递过来的内容)，第二个参数ctx（attr(非prop属性),emit,slots（render()函数需要调用，一般使用Jsx））
  setup() {
    
  },
})
</script>
